# Changelog

## 0.0.1a1

- Initial release: engine registry, `VoiceCloner` facade, Chatterbox ONNX adapter
- CLI: `vconnx clone`, `vconnx list`
- OPM plugin stub (`opm.vc` entry-point)
- Audio-to-audio only; no TTS surface
